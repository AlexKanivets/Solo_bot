# Модуль управления заморозкой подписок для администраторов

## Описание

Этот модуль добавляет функциональность заморозки и разморозки подписок пользователей для администраторов бота. В отличие от пользовательской функции заморозки, которая включается параметром `TOGGLE_CLIENT`, данный модуль всегда активен для администраторов и доступен в меню управления подпиской.

**ВАЖНО:** Модуль использует технику **monkey patching** для интеграции с существующим кодом БЕЗ ИЗМЕНЕНИЯ исходных файлов в `handlers/`. Это позволяет добавить функциональность полностью модульно.

## Функциональность

### Заморозка подписки
- Администратор может заморозить подписку пользователя
- При заморозке подписка отключается на сервере
- Оставшееся время подписки сохраняется
- Пользователь не может использовать подписку в замороженном состоянии

### Разморозка подписки
- Администратор может разморозить подписку пользователя
- При разморозке подписка включается на сервере
- Оставшееся время подписки восстанавливается
- Пользователь снова получает доступ к подписке

## Технические детали

### Monkey Patching

Модуль использует monkey patching для добавления функциональности:

1. **Что патчится:** Функция `build_key_edit_kb` в модуле `handlers.admin.users.keyboard`
2. **Как:** Оригинальная функция оборачивается в wrapper, который:
   - Вызывает оригинальную функцию
   - Добавляет вызов системы хуков
   - Вставляет кнопки из модулей в результирующую клавиатуру
3. **Когда:** При загрузке модуля (import time)

### Преимущества подхода:
- ✅ Не требует изменения исходных файлов handlers/
- ✅ Полностью модульное решение
- ✅ Легко отключить (просто отключить модуль)

### Недостатки подхода:
- ⚠️ Может сломаться при обновлении handlers/
- ⚠️ Менее прозрачен чем прямая интеграция
- ⚠️ Требует осторожности при отладке

## Использование

1. Найдите пользователя через админ-панель (поиск по ID, username или пересылке сообщения)
2. Выберите подписку пользователя в списке его подписок
3. В меню управления подпиской появится кнопка:
   - **❄️ Заморозить подписку** - если подписка активна
   - **🔓 Разморозить подписку** - если подписка заморожена
4. Подтвердите действие
5. Статус подписки обновится

## Расположение кнопки

Кнопка управления заморозкой располагается в меню редактирования подписки после кнопки "♻️ Сбросить трафик".

## Логирование

Все действия по заморозке/разморозке подписок логируются с указанием:
- ID администратора, выполнившего действие
- Email подписки
- ID пользователя-владельца подписки
- Результат операции

При применении monkey patching также логируется успешность операции.

## Настройки

Файл `settings.py` содержит единственную настройку:
- `ADMIN_SUBSCRIPTION_TOGGLE_ENABLED` - включение/выключение модуля (по умолчанию: `"true"`)

## Хуки

### admin_key_edit
Добавляет кнопку управления заморозкой в меню редактирования подписки.

**Параметры:**
- `email` (str) - Email подписки
- `tg_id` (int) - Telegram ID владельца
- `is_frozen` (bool) - Статус заморозки

**Возвращает:**
Словарь с кнопкой и инструкцией по размещению:
```python
{
    "after": callback_data,  # После какой кнопки вставить
    "button": InlineKeyboardButton(...)  # Сама кнопка
}
```

## Обработчики

- `admin_freeze_subscription` - показ диалога подтверждения заморозки
- `admin_freeze_subscription_confirm` - выполнение заморозки
- `admin_unfreeze_subscription` - показ диалога подтверждения разморозки
- `admin_unfreeze_subscription_confirm` - выполнение разморозки

## Зависимости

Модуль использует существующие функции из основного кода:
- `mark_key_as_frozen` / `mark_key_as_unfrozen` - управление статусом в БД
- `toggle_client_on_cluster` - включение/выключение клиента на сервере
- `renew_key_in_cluster` - обновление подписки при разморозке
- `get_key_details` - получение информации о подписке

## Отладка

Если модуль не работает, проверьте логи на наличие сообщений:
- `[AdminSubscriptionToggle] Применение monkey patching...`
- `[AdminSubscriptionToggle] ✅ Monkey patching успешно применен`
- `[AdminSubscriptionToggle] ❌ Ошибка при применении monkey patching: ...`

При ошибках monkey patching модуль может не добавить кнопку в меню, но это не сломает бота.

## Альтернативный подход

Если monkey patching вызывает проблемы, можно интегрировать модуль напрямую:
1. В `handlers/admin/users/keyboard.py` добавить вызов хуков в `build_key_edit_kb`
2. Сделать функцию асинхронной
3. Обновить все вызовы функции с `await`

Это более надежно, но требует изменения исходного кода.

## Версия

Текущая версия модуля: **1.0.0**

## История изменений

### 1.0.0 (2025-11-10)
- ✨ Реализация через monkey patching (без изменения handlers/)
- ✨ Добавлена заморозка/разморозка подписок администратором
- ✨ Интеграция через систему хуков
- 📝 Полная документация

# Модуль управления заморозкой подписок для администраторов

## Описание

Этот модуль добавляет функциональность заморозки и разморозки подписок пользователей для администраторов бота. В отличие от пользовательской функции заморозки, которая включается параметром `TOGGLE_CLIENT`, данный модуль всегда активен для администраторов и доступен в меню управления подпиской.

## Функциональность

### Заморозка подписки
- Администратор может заморозить подписку пользователя
- При заморозке подписка отключается на сервере
- Оставшееся время подписки сохраняется
- Пользователь не может использовать подписку в замороженном состоянии

### Разморозка подписки
- Администратор может разморозить подписку пользователя
- При разморозке подписка включается на сервере
- Оставшееся время подписки восстанавливается
- Пользователь снова получает доступ к подписке

## Использование

1. Найдите пользователя через админ-панель (поиск по ID, username или пересылке сообщения)
2. Выберите подписку пользователя в списке его подписок
3. В меню управления подпиской появится кнопка:
   - **❄️ Заморозить подписку** - если подписка активна
   - **🔓 Разморозить подписку** - если подписка заморожена
4. Подтвердите действие
5. Статус подписки обновится

## Расположение кнопки

Кнопка управления заморозкой располагается в меню редактирования подписки после кнопки "♻️ Сбросить трафик".

## Логирование

Все действия по заморозке/разморозке подписок логируются с указанием:
- ID администратора, выполнившего действие
- Email подписки
- ID пользователя-владельца подписки
- Результат операции

## Настройки

Файл `settings.py` содержит единственную настройку:
- `ADMIN_SUBSCRIPTION_TOGGLE_ENABLED` - включение/выключение модуля (по умолчанию: `"true"`)

## Технические детали

### Хуки
- `admin_key_edit` - добавляет кнопку управления заморозкой в меню редактирования подписки

### Обработчики
- `admin_freeze_subscription` - показ диалога подтверждения заморозки
- `admin_freeze_subscription_confirm` - выполнение заморозки
- `admin_unfreeze_subscription` - показ диалога подтверждения разморозки
- `admin_unfreeze_subscription_confirm` - выполнение разморозки

## Зависимости

Модуль использует существующие функции из основного кода:
- `mark_key_as_frozen` / `mark_key_as_unfrozen` - управление статусом в БД
- `toggle_client_on_cluster` - включение/выключение клиента на сервере
- `renew_key_in_cluster` - обновление подписки при разморозке
- `get_key_details` - получение информации о подписке

## Версия

Текущая версия модуля: **1.0.0**

# Руководство по использованию модуля заморозки подписок для администраторов

## 📋 Краткое описание

Модуль **Admin Subscription Toggle** добавляет возможность заморозки и разморозки подписок пользователей администраторами бота. Эта функция всегда доступна администраторам независимо от параметра `TOGGLE_CLIENT`.

## 🎯 Основные возможности

✅ **Заморозка подписки пользователя**
- Подписка отключается на сервере
- Оставшееся время сохраняется
- Пользователь не может использовать подписку

✅ **Разморозка подписки пользователя**  
- Подписка включается на сервере
- Оставшееся время восстанавливается
- Пользователь получает доступ обратно

✅ **Независимость от настроек пользователя**
- Работает всегда, независимо от `TOGGLE_CLIENT`
- Доступна только администраторам

## 🚀 Как использовать

### Шаг 1: Найти пользователя
В админ-панели выберите "🔍 Поиск пользователя" и введите:
- ID пользователя
- Username пользователя  
- Или перешлите сообщение от пользователя

### Шаг 2: Выбрать подписку
В профиле пользователя выберите нужную подписку из списка.

### Шаг 3: Управление заморозкой
В меню управления подпиской вы увидите:
- **❄️ Заморозить подписку** - если подписка активна
- **🔓 Разморозить подписку** - если подписка заморожена

### Шаг 4: Подтверждение
После нажатия на кнопку появится диалог подтверждения с описанием действия. Нажмите "✅ Применить" для выполнения операции.

## 📊 Отображение статуса

В информации о подписке теперь отображается статус:
- **✅ Активна** - подписка работает нормально
- **❄️ Заморожена** - подписка заморожена администратором

## ⚙️ Настройки модуля

Модуль настраивается через файл `modules/admin_subscription_toggle/settings.py`:

```python
# Включение/выключение модуля (true/false)
ADMIN_SUBSCRIPTION_TOGGLE_ENABLED = "true"
```

## 📝 Логирование

Все действия по заморозке/разморозке записываются в лог с информацией:
- Кто выполнил действие (ID администратора)
- Какая подписка была изменена (email)
- Для какого пользователя (tg_id)
- Результат операции

## 🔍 Техническая информация

### Расположение файлов модуля
```
modules/admin_subscription_toggle/
├── __init__.py          # Инициализация модуля
├── VERSION              # Версия модуля (1.0.0)
├── settings.py          # Настройки модуля
├── hooks.py             # Регистрация хуков
├── router.py            # Обработчики команд
└── README.md            # Документация модуля
```

### Изменения в основном коде

1. **handlers/admin/users/keyboard.py**
   - Функция `build_key_edit_kb` стала асинхронной
   - Добавлен вызов хука `admin_key_edit`

2. **handlers/admin/users/users_handler.py**
   - Добавлено отображение статуса заморозки в информации о подписке
   - Обновлены вызовы `build_key_edit_kb` с `await`

## ❓ Часто задаваемые вопросы

**В: Чем отличается от пользовательской заморозки?**
О: Пользовательская заморозка включается параметром `TOGGLE_CLIENT` и доступна самим пользователям. Админская заморозка работает всегда и доступна только администраторам.

**В: Можно ли отключить модуль?**
О: Да, установите `ADMIN_SUBSCRIPTION_TOGGLE_ENABLED = "false"` в файле настроек.

**В: Что происходит с трафиком при заморозке?**
О: Трафик не сбрасывается, он остается на текущем уровне.

**В: Сколько времени сохраняется при заморозке?**
О: Сохраняется всё оставшееся время подписки на момент заморозки.

## 📞 Поддержка

При возникновении проблем проверьте логи бота. Все ошибки модуля помечаются префиксом `[AdminSubscriptionToggle]`.

---

**Версия документа:** 1.0.0  
**Дата создания:** 2025-11-10
